import { ContentViewChildTypePipe } from './content-view-child-type.pipe';

describe('ContentViewChildTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ContentViewChildTypePipe();

    expect(pipe).toBeTruthy();
  });
});
