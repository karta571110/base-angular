import { CustomTdDirective } from './custom-td.directive';

describe('Directive: CustomTd', () => {
  it('should create an instance', () => {
    const directive = new CustomTdDirective();

    expect(directive).toBeTruthy();
  });
});
