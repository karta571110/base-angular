import { CustomThDirective } from './custom-th.directive';

describe('Directive: CustomTh', () => {
  it('should create an instance', () => {
    const directive = new CustomThDirective();

    expect(directive).toBeTruthy();
  });
});
